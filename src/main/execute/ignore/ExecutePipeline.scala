package borb.execute

import spinal.core._
import spinal.lib.misc.pipeline._
import scala.collection.mutable.ArrayBuffer

// case class ExecutePipeline(node: CtrlLink) extends Area {
//   val eus = ArrayBuffer[ExecutionUnit]()
//
//   // for (i <- 0 until euCount) {
//   //   eus += new ExecutionUnit(null) // FIXME: regfile should be passed here
//   // }
//
//   // val writeback = new WriteBackService(euCount)
//   // writeback.build(this)
// }

// case class ExecutePipeline(node : CtrlLink) extends ExecutionUnit with Area {

// override val FUs = ArrayBuffer[FunctionalUnit]()
// add(IntAlu(node))
// add(Branch(node))
// add(AGU(node))

// }
